services:
  app:
    build:
      context: .
      target: runtime
    working_dir: /app
    environment:
      HOST: ${HOST:-0.0.0.0}
      PORT: ${PORT:-3000}
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
